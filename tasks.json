{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile & Run",
            "type": "shell",
            "command": "python",
            "args": [
                "${workspaceFolder}\\.vscode\\scripts\\compile.py",
                "${file}",
                "&&",
                "python",
                "${workspaceFolder}\\.vscode\\scripts\\run.py",
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Run",
            "type": "shell",
            "command": "python",
            "args": [
                "${workspaceFolder}\\.vscode\\scripts\\run.py",
                "${file}"
            ],
            "group": {
                "kind": "test",
                "isDefault": true
            }
        },
        {
            "label": "Compile for Debug",
            "type": "shell",
            "command": "g++",
            "args": [
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}",
                "-g3",
                "-Wall",
                "-Wextra",
                "-fno-stack-limit"
            ]
        }
    ]
}